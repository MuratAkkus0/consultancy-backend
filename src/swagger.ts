// The OpenAPI document is built from Zod schemas — see src/lib/openapi.ts.
// Each *.routes.ts file self-registers its endpoints via registerRoute().
export { buildOpenApiDocument } from "./lib/openapi.js";
