services:
  db:
    image: postgres:18.4-alpine3.23
    container_name: postgres-db
    env_file:
      - ./.env
    ports:
      - 9002:5432
    volumes:
      - ./postgres-data:/var/lib/postgresql
volumes:
  postgres-data:
